# Inbox

Rough captures. Name them `YYYY-MM-DD-short-slug.md` and don't think about where they belong.

The point is that adding something costs thirty seconds. Structure can come later, if the
entry turns out to deserve it — and plenty won't, which is fine. An inbox that never empties
isn't a backlog here; most captures are worth exactly as much as they cost.
